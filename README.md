# My-ETL-SpaceX-Launch-Data-Project
🚀 SpaceX Launch ETL Pipeline  A Python-based ETL project using the SpaceX public API  This project demonstrates a full ETL workflow (Extract → Transform → Load) using Python and the SpaceX v4 public API. The goal is to extract rocket launch data, transform nested JSON into a clean DataFrame, and analyze success vs. failures for this dataset.
